version: '2'
services:
  chatty-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/chatty/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=chatty
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
